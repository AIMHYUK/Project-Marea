# 개발 환경

> 상태: 진행중 · 갱신 2026-09-08 · 관련: [1차 구현 분담](1차_구현_분담.md), [설계 결정](설계_결정.md)
> 무엇: 이 프로젝트에 이미 깔려 있는 것과, 그 때문에 코드에서 지켜야 하는 제약.

2026-08-28 시점 `D:\UnityPractice\Proejct_Marea` 실측값이다.

## 버전

| | |
|---|---|
| Unity | **6000.3.6f1** |
| 렌더 파이프라인 | URP **17.3.0** |
| 씬 | `Assets/Scenes/SampleScene.unity` 하나 (기본 템플릿 상태) |

## 지켜야 하는 제약

### 레거시 `Input` 클래스를 쓰면 안 된다

`ProjectSettings.asset`의 `activeInputHandler: 1` — **Input System Package 전용**이다.
`Input.GetAxis()`, `Input.GetMouseButtonDown()` 은 컴파일은 되지만 실행 시
`InvalidOperationException` 이 난다.

```csharp
// 안 됨
if (Input.GetMouseButtonDown(0)) { ... }

// 이렇게
using UnityEngine.InputSystem;
if (Mouse.current.leftButton.wasPressedThisFrame) { ... }
```

`Assets/InputSystem_Actions.inputactions` 가 이미 있다 (Unity 6 기본 템플릿).
`Move` / `Look` / `Attack` 액션이 들어 있으므로 WASD·클릭은 여기에 붙이면 된다.

### NavMesh는 쓸 수 있다

`com.unity.ai.navigation` **2.0.9** 설치됨. 클릭 이동은 `NavMeshAgent`로 간다.

WASD와 클릭 이동이 한 캐릭터에 같이 있으므로 충돌 처리가 필요하다.

- WASD 입력이 들어오면 `agent.ResetPath()` 로 클릭 이동을 취소한다
- 수동 이동도 `transform.position` 직접 대입이 아니라 `agent.Move()` 를 쓴다.
  안 그러면 NavMesh 밖으로 나가서 다음 클릭 이동이 실패한다

### 한글 폰트 아틀라스에 없는 글자가 있다 `(+9/8)`

UI 텍스트는 `Assets/Fonts/Pretendard-Bold SDF` 를 쓴다. 이 에셋은 이렇다.

| | |
|---|---|
| `atlasPopulationMode` | **Static** — 없는 글자가 런타임에 추가되지 않는다 |
| 글리프 수 | 2296 |
| 폰트 에셋 fallback | **0개** |
| TMP 전역 fallback (`TMP Settings`) | **0개** |

**Static + fallback 0 이라 없는 글자는 구제될 길이 없다.** TMP가 공백(U+0020)으로
바꿔 그리고 `character not found` 경고를 **매 프레임** 찍는다. 화면은 그럭저럭 보이는데
Console이 도배돼서 진짜 에러가 묻힌다.

**한글은 대체로 괜찮다.** 음절 2201 / 11172 (19%) 뿐이지만 상용 음절 집합이라
실사용 단어는 거의 다 된다 — 게임에 나올 법한 40단어(감자·스튜·꼬치·정산·업그레이드…)를
넣어보니 `뷁` 하나만 실패했다.

**문제는 기호다. 아래는 전부 없다.**

```
· — – … → ← ★ ☆ ♥ ♡ ° ₩ 「」『』《》〈〉 “ ” ‘ ’ • ※ ▶ ◀ ▲ ▼ ■ □ ● ○ × ÷ ± ≤ ≥ ≠ ∞
```

- **`₩` 가 없다.** 경영 게임인데 원화 기호를 못 쓴다. 금액은 `1200 G` 나 `1,200원` 으로 쓴다.
- 예쁜 따옴표 `“ ”` `‘ ’` 도 없다. 곧은 따옴표 `" '` 를 쓴다.
- **ASCII는 전부 있다** — `% $ ~ | / - + = ( ) [ ] { } < > , . ? ! : ;` 등은 마음껏 써도 된다.

실제로 밟았다 `(9/8)`. `WorldClockUI` 의 표시 형식을 `낮 · 06:00` 으로 잡았다가
가운뎃점(U+00B7) 때문에 경고가 도배됐다. `낮 06:00` 으로 바꿨다.

> 글자를 늘리려면 폰트 에셋의 문자 목록에 추가하고 `Update Atlas Textures` 를 하거나
> `atlasPopulationMode` 를 Dynamic으로 바꾼다. **다만 이 폰트 에셋은 A·B가 같이 쓰는
> 공유물이다 — 바꾸기 전에 말한다.**

### 그 밖에 깔려 있는 것

`com.unity.inputsystem` 1.18.0 · `com.unity.ugui` 2.0.0 · `com.unity.timeline` 1.8.10 ·
`com.unity.test-framework` 1.6.0 · `com.unity.visualscripting` 1.9.9 ·
`com.unity.multiplayer.center` 1.0.1 · `com.unity.nuget.newtonsoft-json` 3.2.2

멀티플레이어·비주얼스크립팅은 쓰지 않는다. 지워도 되지만 급하지 않다.

## MCP for Unity

유니티 에디터를 Claude Code에 붙이는 브리지. **`Assets/MCPForUnity/` 에 v10.1.2 로
넣어뒀다** (`com.coplaydev.unity-mcp`, https://github.com/CoplayDev/unity-mcp).

### 왜 패키지가 아니라 에셋인가

원래는 `manifest.json` 에 git URL로 들어 있었다.

```json
"com.coplaydev.unity-mcp": "https://github.com/CoplayDev/unity-mcp.git?path=/MCPForUnity#main"
```

`#main` 은 **버전이 고정되지 않는다.** 패키지를 다시 풀 때마다 다른 커밋을 물어올 수
있어서, 내 로컬에서도 브리지 버전이 조용히 바뀐다. 그래서 캐시에 받아진 10.1.2를
`Assets/` 로 옮기고 manifest 항목은 지웠다. 이제 버전이 움직이지 않는다.

**같이 바꾼 것**: 에셋 폴더에 들어간 `package.json` 은 패키지 매니저가 안 읽으므로
의존성이 따라오지 않는다. git 패키지가 끌어오던 `com.unity.nuget.newtonsoft-json` 을
manifest에 **직접 추가**했다 (3.2.2). 이게 없으면 MCP 쪽이 컴파일되지 않는다.

이전 manifest는 `Packages/manifest.json.bak` 에 남겨뒀다. `.gitignore` 에서 `*.bak` 을
빼놨으니 커밋에는 안 올라간다.

### 커밋하지 않는다

`Assets/MCPForUnity/` 와 그 `.meta` 는 `.gitignore` 로 제외했다. 게임 코드가 아니라
**에디터 도구**이고, 게임 쪽에서 참조하는 게 하나도 없다. 커밋하면 MCP를 안 쓰는
사람도 매번 컴파일하게 되고 버전 올릴 때마다 diff가 수백 파일로 잡힌다. 쓰는 사람이
각자 넣는다.

`.meta` 를 같이 빼는 게 중요하다. 폴더 없이 메타만 남으면 상대 쪽에 붙을 데 없는
파일이 굴러다닌다. "`.meta` 는 반드시 커밋" 규칙의 유일한 예외다.

`com.unity.nuget.newtonsoft-json` 은 manifest에 그대로 둔다. MCP를 안 받는 사람에게도
해가 없고, manifest가 사람마다 갈리는 게 더 골치다.

### 다시 넣는 방법

이 프로젝트를 새로 받아서 MCP를 쓰려면 유니티에서 Package Manager > Add package from
git URL 로 `https://github.com/CoplayDev/unity-mcp.git?path=/MCPForUnity` 를 넣고,
`Library/PackageCache/com.coplaydev.unity-mcp@*/` 를 `Assets/MCPForUnity/` 로 복사한 뒤
manifest 항목을 지운다. 버전을 고정하려면 이 순서여야 한다.

### 붙이는 절차

1. 유니티 메뉴 `Window > MCP for Unity`
2. **Auto-Setup** — 클라이언트 설정을 대신 써준다. `claude` CLI가 PATH에 있어야 하고,
   서버 실행에 Python + uv/uvx 가 필요하다
3. Unity Bridge가 `Stopped` 면 **Start Bridge**

### 포트

| | |
|---|---|
| Unity Bridge | **6400** 기본값 (충돌하면 자동 변경 — 창에 실제 포트가 뜬다) |
| MCP 서버 | **6500** |

안 붙으면 이 둘부터 본다.

> 미확인: Auto-Setup이 MCP 등록을 user 스코프에 쓰는지 project 스코프에 쓰는지
> 확인하지 않았다. `C:\Users\choju` 에서 claude를 띄웠을 땐 `/mcp` 가 비어 있었다.
> 이 프로젝트 폴더에서 띄워보고, 그래도 없으면 `claude mcp list` 로 스코프를 본다.

### 플레이 모드 테스트 — 에디터가 비포커스면 시간이 거의 안 간다 `(+9/8)`

**`Run In Background` 가 꺼져 있다** (`ProjectSettings/ProjectSettings.asset` 의
`runInBackground: 0`). 그래서 **유니티 창이 포커스를 잃으면 플레이 모드의 `Update` 가
거의 멈춘다.** 알트탭 해도 그렇고, MCP로 원격 조작하면 창이 항상 뒤에 있으니 항상 그렇다.

`WorldClock` 을 검증하다 걸렸다 — `play` 걸고 30초를 기다렸는데 게임 내 시각이
**06:02 → 06:06** 밖에 안 갔다. 하루를 24초로 줄여놨으니 하루가 한 바퀴 돌았어야 했다.

**이게 위험한 건 코드 버그처럼 보이기 때문이다.** "타이머가 안 도네"로 읽고 멀쩡한
`Update` 를 뜯게 된다. 시간 기반인 것(`WorldClock`, B의 영업 타이머)은 전부 이 함정을 밟는다.

| 상황 | 대처 |
|---|---|
| 사람이 직접 눌러 볼 때 | **Game 뷰를 클릭해 포커스를 준 채로 둔다.** 이게 기본 |
| MCP로 검증할 때 | 플레이 중에 `execute_code` 로 `Application.runInBackground = true;` 한 줄 |
| 영구히 켜고 싶으면 | Project Settings → Player → Resolution and Presentation → **Run In Background** |

두 번째의 `Application.runInBackground` 는 **런타임 전용 프로퍼티**다. 그 플레이 세션에만
살고 플레이를 끄면 사라지며, `ProjectSettings.asset` 을 건드리지 않는다. 검증용으로 쓰기 좋다.

세 번째는 다르다. **`PlayerSettings.runInBackground` 는 파일에 저장되고 그 파일은
커밋된다 — 팀 공용 설정이고 빌드 동작까지 바뀐다.** 혼자 켜지 말고 팀에 말하고 켠다.

## 버전 관리

`(+8/31)` **저장소를 만들고 원격에 연결했다.**

```
origin  https://github.com/AIMHYUK/Project-Marea
main    → origin/main 추적
```

GitHub가 만든 초기 커밋(`README.md` + 템플릿 `.gitignore`) 위에 얹었다.
`git reset origin/main` 으로 히스토리를 이어붙였기 때문에 나중에
`unrelated histories` 로 막히지 않는다.

**`.gitattributes` 도 같이 넣었다** — 줄바꿈 규칙을 개인 `core.autocrlf` 가 아니라
저장소 안에 둔다. 설정이 다른 사람이 커밋하면 내용은 그대로인데 전 줄이 바뀐 diff가
생기고, 씬 파일에서 그러면 충돌 해결이 불가능해진다.
UnityYAMLMerge 설정은 각자 해야 해서 파일 안에 주석으로만 남겼다.

### 브랜치 전략 `(+8/31)`

- `main` — 항상 유니티에서 열리는 상태를 유지한다. **직접 커밋하지 않는다**
- `feature/<이슈번호>-<짧은이름>` — 작업 단위 하나에 브랜치 하나.
  예: `feature/1-agent-mover`
- PR로 머지한다. 본문에 `Closes #N` 을 넣으면 머지될 때 이슈가 자동으로 닫힌다
- 이슈는 `gh issue create`, PR은 `gh pr create` 로 만든다 (GitHub CLI)

⚠️ **씬 파일(`.unity`)을 건드리는 작업은 시작 전에 말한다.** UnityYAMLMerge를 아직
안 켰기 때문에 충돌하면 손으로 푼다. 아트가 씬에 오브젝트를 배치하는 동안 다른 사람이
같은 씬에 무언가를 넣으면 그때 터진다. 브랜치를 나눠도 이건 안 막아준다 —
**머지 시점에 똑같이 충돌한다.**

`.gitignore` 에서 일부러 **커밋하도록 둔 것**:

- `.meta` 파일 — 빠지면 팀원 쪽에서 참조가 전부 끊긴다
- `Packages/` (manifest, packages-lock 둘 다), `ProjectSettings/`, `Docs/`

제외한 것은 `Library/`, `Temp/`, `Logs/`, `UserSettings/`, `*.csproj`, `*.sln`,
빌드 산출물, `*.bak`, 그리고 `Assets/MCPForUnity/` (위 참고).
**`Library/` 를 빼먹고 커밋하면 저장소가 수 GB로 불어난다.**

## 아직 안 된 것

### 폴더 구조 미정

제안:

```
Assets/Scripts/
  Core/        IInteractable, IInteractor, InteractableBase   (A)
  Data/        IngredientData, MenuData, CropData             (공유 / A)
  Player/      PlayerController, ClickSelector, CameraFollow  (A)
  Field/       Warehouse, FarmPlot                            (A)
  Shop/        GameManager, Customer, Table, CookingStation…  (B)
  UI/          OrderBubbleUI, ResultPanelUI                   (B)
```

네임스페이스는 `Marea.Core`, `Marea.Data`, `Marea.Player`, `Marea.Field`, `Marea.Shop`.
폴더로 소유자가 갈리면 충돌이 눈에 보인다.
